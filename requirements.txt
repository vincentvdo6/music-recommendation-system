# Web service
fastapi>=0.111,<1
uvicorn[standard]>=0.29,<1
httpx>=0.27,<1
python-dotenv>=1.0,<2
orjson>=3.9,<4
anyio>=4,<5
cachetools>=5.3,<6
slowapi>=0.1.9,<1

# Embeddings + retrieval
# numpy<2 is load-bearing: gensim/annoy wheels break on the numpy 2 ABI.
numpy>=1.24,<2
gensim>=4.3,<5
annoy>=1.17,<2

# Ranking + metadata
lightgbm>=4.1,<5
pandas>=2.1,<3
pyarrow>=15,<24
scikit-learn>=1.3,<2

# Item-NCF scoring (optional: the app runs without it, ncf features zero out)
torch>=2.0,<3

# Tests
pytest>=8.0,<9
pytest-asyncio>=0.23,<1
