[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "music-rec"
version = "0.1.0"
description = "Music recommendation system with embeddings and neural ranking"
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
authors = [
    {name = "Music Rec Team", email = "team@musicrec.com"},
]
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    "sqlalchemy>=2.0.23",
    "alembic>=1.13.0",
    "asyncpg>=0.29.0",
    "redis>=5.0.1",
    "boto3>=1.34.0",
    "httpx>=0.25.2",
    "numpy>=1.24.0",
    "scipy>=1.11.0",
    "faiss-cpu>=1.7.4",
    "torch>=2.1.0",
    "torchaudio>=2.1.0",
    "librosa>=0.10.1",
    "xgboost>=2.0.0",
    "scikit-learn>=1.3.0",
    "prometheus-client>=0.19.0",
    "structlog>=23.2.0",
    "pyjwt>=2.8.0",
    "cryptography>=41.0.0",
    "pillow>=10.1.0",
    "mutagen>=1.47.0",
    "pyyaml>=6.0.1",
    "click>=8.1.7",
    "python-multipart>=0.0.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.3",
    "pytest-asyncio>=0.21.1",
    "pytest-cov>=4.1.0",
    "httpx>=0.25.2",
    "pre-commit>=3.6.0",
    "ruff>=0.1.6",
    "mypy>=1.7.1",
    "types-PyYAML>=6.0.12",
    "types-redis>=4.6.0",
]

[tool.hatch.version]
path = "music_rec/__init__.py"

[tool.ruff]
target-version = "py311"
line-length = 88
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings  
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"]

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
disallow_any_generics = true
disallow_incomplete_defs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_return_any = true
strict_equality = true

[[tool.mypy.overrides]]
module = [
    "faiss.*",
    "librosa.*", 
    "mutagen.*",
    "xgboost.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
addopts = "--cov=. --cov-report=html --cov-report=term-missing"

[tool.coverage.run]
source = ["."]
omit = [
    "*/tests/*",
    "*/__pycache__/*",
    "*/venv/*",
    "*/env/*",
    "setup.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]